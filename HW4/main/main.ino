#include"library.h"
int rainbow[][3] = {{0,255,255},{0,255,128},{0,255,0},{255,255,0},{255,0,255},{180,125,255},{107,44,255}}; //控制共陽LED彩虹顏色的RBG順序，RGB的數值要設相反的，也就是紅、藍、綠的值要設255 - 原先的數值，比如紅色要設(0,255,255)。
int color[][3] = {{255, 0 , 0},{255, 127, 0},{255, 255, 0},{0, 255, 0},{0, 0, 255},{75, 0, 130},{148, 0, 211}}; //控制WS2832B彩虹顏色的RGB順序。

void setup() {

  initial();
}

void loop() 
{ 
  for(int i = 0 ; i < 7 ; i++)
  {
    run(rainbow[i],color[i]);
  }
}